import { FilterCorporatePipe } from './filter-corporate.pipe';

describe('FilterCorporatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCorporatePipe();
    expect(pipe).toBeTruthy();
  });
});
